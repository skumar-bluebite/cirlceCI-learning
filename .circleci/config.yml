version: 2.1

jobs:
  hello-job:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - run:
          name: Print Hello World
          command: echo "Hello, World!"

workflows:
  say-hello-workflow:
    jobs:
      - hello-job
